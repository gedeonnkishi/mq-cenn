[build-system]
requires = [
    "setuptools>=68,<82",
    "wheel",
    "pybind11>=2.11"
]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
addopts = "-ra"
